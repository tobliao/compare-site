---
import type { Trend } from "@price/core/types";

interface Props {
  trend: Trend;
}

const { trend } = Astro.props;
---

<article class="trend-card">
  <h3>{trend.keyword}</h3>
  <p>{trend.approxTraffic} · {new Date(trend.observedAt).toLocaleDateString("zh-TW")}</p>
</article>
